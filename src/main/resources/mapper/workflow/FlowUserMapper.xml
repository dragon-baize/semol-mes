<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.senmol.mes.workflow.mapper.FlowUserMapper">

    <select id="getByPid" resultType="com.senmol.mes.workflow.entity.FlowUser">
        SELECT
            fu.flow_id,
            fu.user_id,
            fu.sort
        FROM
            work_flow_user fu
            LEFT JOIN work_flow_record fr ON fu.flow_id = fr.flow_id
        WHERE
            fr.id = #{pid}
          AND fr.deleted = 0
        ORDER BY
            fu.sort
    </select>

</mapper>

